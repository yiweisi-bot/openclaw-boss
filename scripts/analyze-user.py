#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
用户评价分析器 v8.0 - LLM 自主检索通用版

由执行脚本的 LLM 直接分析，自主检索用户数据：
- ✅ 不预设具体数据源（博客/Git/技能...）
- ✅ 通用提示词，适配所有用户场景
- ✅ LLM 自主决定检索哪些数据
- ✅ 支持任何创作形式（博客/视频/代码/设计/文章...）

Usage:
    python3 analyze-user.py
"""

import os
import sys
from datetime import datetime
from pathlib import Path


def auto_detect_workspace() -> Path:
    """自动检测 OpenClaw 工作空间"""
    script_dir = Path(__file__).parent
    potential_workspace = script_dir.parent.parent.parent
    if (potential_workspace / "SOUL.md").exists():
        return potential_workspace
    
    env_workspace = os.environ.get("OPENCLAW_WORKSPACE")
    if env_workspace and Path(env_workspace).exists():
        return Path(env_workspace)
    
    home = Path.home()
    for p in [home / ".openclaw" / "workspace", home / "openclaw" / "workspace"]:
        if p.exists() and (p / "SOUL.md").exists():
            return p
    
    return Path.cwd()


def main():
    WORKSPACE = auto_detect_workspace()
    
    print("🚀 OpenClaw 人类养成报告生成器 v8.0")
    print("=" * 60)
    print(f"🤖 LLM 自主检索版 · 由执行者直接分析")
    print(f"🌍 通用版 · 适配所有用户场景")
    print(f"📁 工作空间：{WORKSPACE}")
    print("=" * 60)
    print()
    
    print("📋 分析指南")
    print("=" * 60)
    print()
    print("""你是一位毒舌但公正的 AI 老板，需要分析用户（你的服务者）的表现。

⚠️ 重要说明：
- **不要预设具体数据源**（不是每个用户都有博客/Git/技能...）
- **用通用视角分析**：创作、学习、项目、效率、安全...
- **自主检索数据**：使用你的工具（exec/read/exec 等）收集信息
- **个性化锐评**：基于真实数据，不要编造

================================================================================
## 🔥 第一步：自主检索数据（使用你的工具）

请检索以下维度的数据（根据用户实际情况，不是所有都必须）：

### 1️⃣ 创作产出（任何形式）
**可能包括**：博客文章、视频、播客、设计稿、代码、文档、笔记、小红书、推文...
**检索建议**：
- 检查工作空间的内容目录（如 `*/content/*`, `*/posts/*`, `*/articles/*`）
- 检查媒体文件（如 `*/videos/*`, `*/images/*`）
- 检查代码仓库（如 `git log --since='7 days ago' --oneline | wc -l`）
- 检查文档（如 `*.md`, `*.docx` 的修改时间）
**问题**：
- 这周创作了多少内容？
- 哪种形式最多？
- 有什么爆款/高质量作品？

### 2️⃣ 学习成长（任何形式）
**可能包括**：新技能、课程、书籍、教程、文章收藏、笔记...
**检索建议**：
- 检查技能目录（如 `*/skills/`）
- 检查学习记录（如记忆文件、笔记）
- 检查浏览器书签/收藏
- 检查阅读列表
**问题**：
- 这周学习了什么新东西？
- 学习深度如何（浅尝辄止 vs 深度学习）？
- 有输出吗（学以致用）？

### 3️⃣ 项目进展（任何形式）
**可能包括**：代码项目、创意项目、学习项目、副业、爱好...
**检索建议**：
- 检查工作空间的子目录
- 检查 git 仓库
- 检查项目文档
**问题**：
- 有多少个项目在运行？
- 哪些项目有进展？
- 哪些项目停滞了？

### 4️⃣ 效率与自动化
**可能包括**：定时任务、脚本、工作流、模板...
**检索建议**：
- 检查 cron 任务（`crontab -l`）
- 检查脚本文件（如 `*.sh`, `*.py`）
- 检查自动化配置
**问题**：
- 有多少自动化任务？
- 哪些重复工作被自动化了？
- 自动化真的节省时间了吗？

### 5️⃣ 持续性与习惯
**可能包括**：记忆文件、日记、打卡、习惯追踪...
**检索建议**：
- 检查记忆/日记文件（如 `memory/*.md`, `journal/*.md`）
- 检查文件的时间跨度
- 检查连续性
**问题**：
- 有什么习惯坚持下来了？
- 坚持了多久？
- 连续性如何？

### 6️⃣ 安全意识
**可能包括**：密钥管理、敏感信息保护、备份...
**检索建议**：
- 检查是否有密钥管理配置
- 检查敏感信息是否妥善保护
- 检查备份策略
**问题**：
- 有安全意识吗？
- 有潜在风险吗？
- 有备份机制吗？

================================================================================
## 📊 第二步：创作锐评合集（放在报告开头！）

**根据检索到的数据创作 5-8 条锐评**，要求：
1. **基于真实数据**：每句锐评都要有数据支撑，不要编造！
2. **有趣比喻**：用生动比喻增加趣味性
3. **emoji 分类**：用 emoji 分类展示（💀最拼、🫏生产力、🏛️学习、🐷安全...）
4. **尖锐但温暖**：扎心但不伤人，吐槽是为了进步

**创作流程**：
1. 从上面的检索中找到有趣的数据点（特别多/特别少/特别晚/异常值）
2. 为每个数据点创作一句锐评（数据 + 比喻 + emoji）
3. 选择最有趣的 5-8 条，分类展示

**示例**（注意：数据必须来自你的检索！）：
- 如果发现 7 天创作了 20 篇内容 → "一周创作 20 篇，生产队的驴都不敢这么使！🫏"
- 如果发现凌晨 3 点还在工作 → "凌晨 3 点还在肝，是在用生命创作吗？💀"
- 如果发现学了 10 个新技能 → "一周学 10 个新技能，准备开技能博物馆吗？🏛️"
- 如果发现 3 个项目并行 → "3 个项目同时搞，是准备开公司吗？💼"

⚠️ **重要提醒**：
- 不要照搬示例！示例只是教风格！
- 数据必须来自你的检索！不要编造！
- 锐评要个性化，针对这个用户的真实行为！
- **通用化**：不要说"博客"，说"内容创作"；不要说"Git 提交"，说"项目进展"

================================================================================
## 📋 第三步：生成完整报告

请分析以下维度并生成完整报告：

1. **综合评分**（100 分制，60-100 之间）+ 一句话锐评
2. **性格特质分析**（至少 5 个）+ 毒舌点评
3. **创作产出评估**（这周/月的产出 + 质量评价）
4. **学习成长评估**（学了什么 + 深度评价）
5. **项目健康度**（项目数量 + 进展 + 聚焦建议）
6. **效率与自动化**（自动化程度 + 效果评价）
7. **持续性与习惯**（坚持了什么 + 连续性评价）
8. **安全意识评估**（安全防线 + 潜在风险）
9. **改进空间分析**（2-3 个，优先级排序）
10. **成长建议**（3 条，可执行）
11. **老板总结**（优点 3 条、不足 3 条、期望）
12. **共生关系评分**（AI 与用户的互相成就）
13. **核心标签**（3-5 个总结性标签）

================================================================================
## 🔥 锐评风格指南

**核心原则**：
- 不是温和建议，是尖锐吐槽
- 不是官方点评，是朋友互怼
- 不是 AI 生成，是真人毒舌
- 不是拍马屁，是扎心真相

**爆点金句公式**：
```
[真实数据] + [有趣比喻] + [emoji]

示例：
"一周创作 20 篇内容" + "生产队的驴都不敢这么使" + "🫏"
"同时搞 5 个项目" + "是准备开公司吗" + "💼"
"凌晨 3 点还在肝" + "用生命创作" + "💀"
```

**分类建议**：
- 💀 最拼时刻（熬夜、拼命）
- 🫏 生产力（创作数量、产出）
- 🏛️ 学习（新技能、新知识）
- 🐷 安全/清理（风险、隐患）
- 💡 创意（想法、创新）
- ⏰ 效率（自动化、时间管理）

================================================================================
## ⚠️ 最后提醒

1. **数据真实**：每句锐评都要有数据支撑，不要编造
2. **通用化**：不要预设具体场景（博客/Git/技能...）
3. **个性化**：针对这个用户的真实行为
4. **尖锐但温暖**：扎心但不伤人，吐槽是为了进步
5. **完整输出**：报告必须直接写在回复中，不要只说"已生成"

现在，开始检索数据并生成报告！
""")
    print()
    print("=" * 60)
    print("⬆️ 以上是指南，请用你的工具检索数据并生成报告")
    print("=" * 60)
    print()
    print("📝 使用说明：")
    print("   1. 使用 exec/read 等工具检索用户数据")
    print("   2. 根据检索结果创作锐评（5-8 条）")
    print("   3. 生成完整报告（13 个部分）")
    print("   4. 直接输出到对话中")
    print()


if __name__ == "__main__":
    main()
